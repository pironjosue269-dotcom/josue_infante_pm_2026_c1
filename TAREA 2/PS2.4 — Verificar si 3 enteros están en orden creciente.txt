#include <stdio.h>

int main(void){
    int a, b, c;

    printf("a b c: ");
    scanf("%d %d %d", &a, &b, &c);

    if (a < b && b < c) printf("SI, creciente\n");
    else printf("NO, creciente\n");

    return 0;
}
