#include <stdio.h>

int main(void){
    double a, b, c, m;

    printf("a b c: ");
    scanf("%lf %lf %lf", &a, &b, &c);

    m = a;
    if (b > m) m = b;
    if (c > m) m = c;

    printf("Mayor: %.2f\n", m);
    return 0;
}
