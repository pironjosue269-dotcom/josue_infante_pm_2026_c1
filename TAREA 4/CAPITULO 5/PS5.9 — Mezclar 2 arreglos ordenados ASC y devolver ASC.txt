#include <stdio.h>

int main(void){
    int N,M;
    int A[200], B[200], C[400];

    printf("N: "); scanf("%d",&N);
    printf("M: "); scanf("%d",&M);

    printf("VEC1 (ASC):\n");
    for(int i=0;i<N;i++){ printf("A[%d]: ", i); scanf("%d",&A[i]); }

    printf("VEC2 (ASC):\n");
    for(int i=0;i<M;i++){ printf("B[%d]: ", i); scanf("%d",&B[i]); }

    int i=0,j=0,k=0;
    while(i<N && j<M){
        if(A[i] <= B[j]) C[k++] = A[i++];
        else C[k++] = B[j++];
    }
    while(i<N) C[k++] = A[i++];
    while(j<M) C[k++] = B[j++];

    printf("Mezclado ASC:\n");
    for(int t=0;t<N+M;t++) printf("%d%s", C[t], (t==N+M-1)?"\n":" ");

    return 0;
}
